# golang-devenv-docker
make docker container to develop golang environment

# usage
make container
```
docker-compose up -d
```

delete container
```
docker-compose down
```

delete container with volume
```
docker-compose down -v
```
